#email-list-members.modal.hide.fade
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h3= t '.header'
  = form_tag send_email_smart_list_path(resource), method: :post do
    .modal-body
      .row-fluid
        .span12
          = label_tag :subject, t('.email')
          = text_field_tag :subject, nil, class: 'span12'
      .row-fluid
        .span12
          %label= t '.content'
          %textarea{ name: 'content', class: 'span12', rows: 9 }
    .modal-footer
      %button.btn.btn-default{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"}= t '.cancel'
      = submit_tag t('.button'), class: 'btn btn-primary'
