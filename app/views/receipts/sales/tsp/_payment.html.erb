request += builder.createTextElement({<%= tsp_international %> data: '<%= tsp_formatted_currency(payment.amount) %> <%= payment.payment_method.name %> - <%= l(payment.created_at, formats: :default) %>\n'});
